README.txt at 2025.05.16 (v3.0)

1. 概要
	立花証券ｅ支店ＡＰＩ（以下 e_api）を python から利用するためのサンプルプログラムです。
	以下機能の動作確認がでます。

	(1) 認証I/F 認証実施、通番及び仮想ＵＲＬをファイル保存及び読出機能
	(2) REQUEST I/F 指定銘柄、指定項目の時価問い合わせ取得機能
	(3) EVENT I/F 指定銘柄の時価配信取得機能
	(4) EVENT I/F WebSocket 指定銘柄の時価配信取得機能

	【変更点】
	e_api（v4r9）より利用設定画面（標準Ｗｅｂ、お客様情報から起動）で
	１．ｅ支店・ＡＰＩ利用有無＝「利用する」を選択しｅ支店・ＡＰＩの利用を宣言
	２．１．で自動生成される認証ＩＤをログインＩ／Ｆの引数に設定
	３．利用設定画面で公開キーを設定（立花証券側サーバで登録された公開キーで仮想ＵＲＬを暗号化し応答）
	４．ログインＩ／Ｆの応答で返される仮想ＵＲＬを利用者が保持する密キーで複合化し利用
	の流れとなります。
	※利用有無＝「利用する」でない（デフォルトは「利用しない」）場合、ログインＩ／Ｆはエラー応答します。

	秘密キー、公開キーは
	５．利用者が自身で手動作成し利用設定画面で作成した公開キー（のみ）を登録
	または
	６．利用設定画面で自動作成し作成後画面に１度だけ表示する秘密キー（のみ）を利用者がファイル等に保存
	の操作が必要となります。
	※秘密キーは利用者側で保存管理頂く手順になりますので立花証券側では保存管理は一切いたしません。
	※公開キーのみ立花証券側サーバに登録保存します。
	※公開キーが未登録の場合、ログインＩ／Ｆはエラー応答します。

	上記記載事項以外の変更はありません（サンプルプログラムも上記依存箇所のみ書き換えています）。

	【注意点】
	e_api（v4r9）に対応した python サンプルプログラムですので e_api（v4r9）意外ではご利用頂けません。
	公開鍵暗号化方式用のライブラリが追加で必要になります。
	「
		% pip install cryptography
	」


2. 使い方
	エディター等で e_api_sample.py を開き以下を変更。

	(1) 認証ＵＲＬ、認証ＩＤ、秘密キー
		「
			gp_url_auth	= "https://demo-kabuka.e-shiten.jp/e_api_v4r9/auth/"
			gp_authid	= "利用設定画面の認証ＩＤ表示（自動生成）値を設定"
			gp_private_key	= b"""
-----BEGIN PRIVATE KEY-----
利用設定画面で登録した公開キーと対の秘密キー（ＰＥＭ形式）を設定
キー利用は暗号化方式「RSA」、暗号化ビット数「2048」または「4096」
暗号（複合）化アルゴリズムは「SHA-256」
-----END PRIVATE KEY-----
"""
		」
		認証URLはデフォルトデモ環境です。
		本番環境やバージョンアップ時など必要に応じ書き換えてご利用ください。

	(2) 機能選択
		「
			gp_proc_request_if	= 0	#! use request i/f sample get market price.
			gp_proc_event_if	= 0	#! use event i/f specify issue_code's and columns.
			gp_proc_event_ws	= 0	#! use event i/f websocket.
		」
		1.(2) 利用時は gp_proc_request_if = 1 に変更。
		1.(3) 利用時は gp_proc_event_if	= 1 に変更。
		1.(4) 利用時は gp_proc_event_ws	= 1 に変更。
		※(3),(4) は  Ctrl^C 押下まで受信ループします。


	(3) HTTPS POST または HTTPS GET の利用選択
		「
			self._p_debug_http_post	= 0	#! request to http-post or http-get.
		」
		上記設定で (1), (2) について 1:POST または 0:GET の切替が可能。

	(4) その他
		その他デバック出力用のフラグ「self._p_debug_print_oxox」は
		必要に応じ ON/OFF を切り替えてご利用ください。


3. 処理内容
	（初回等、保存ファイルが無い場合）ログイン認証を実行し通番と仮想URLをファイルに保存。
	その後、1.(2)-(4)について指定処理を実施。
	1.(2) の場合、時価問合取得要求送信後、応答内容（JSON）を画面に出力し
	通番（p_no）を再度保存ファイルに保存。

	保存ファイル名は YYYYMMDD_e_api_sampl.txt です。

	保存ファイルがある場合は該当ファイルから通番、仮想URLを取得し認証をしません。
	仮想URL の無効化などにより再度認証したい場合は
	保存ファイルを削除してから起動してください。

	保存ファイルが無い場合は認証し仮想URLを取得、通番は初期化します。
	保存ファイル名には日付（YYYYMMDD）を付けています。
	翌日になるとファイル名が変わり読み込まれないため認証します。


4. 実行方法
	% python e_api_sample.py


5. 実行環境
	python、及び import 利用の各ライブラリモジュールをインストールする。
	例：
		% pip install urllib3
		% pip install requests
		% pip install asyncio
		% pip install websockets
		% pip install cryptography
		など。

	windows の場合は cmd.exe で
	% python
	と入力しリターン押下すると python のインストーラが起します。
	起動したインストーラ画面に従いインストールを実施ください。
	※インストーラが起動しない場合、
	　python のパッケージをダウンロードするなどしインストールしてください。

	その後、cmd.exe で上記ライブラリをインストールしてください。
	※動作環境構築については自己責任で実施願います。


6. 動作確認環境
	以下環境で動作確認済です。
	(1) Python 3.13.5 on CentOS Linux release 7.3.1611 (Core)
	(2) Python 3.8.12 on FreeBSD 13.0-RELEASE-p14 (GENERIC)
	(3) Python 3.13.5 on Window 11 Pro 24H2
	※利用ＯＳやインストールされている各種アプリケーションを含むソフトウエア、
　　　　　python（含む import ライブラリ）等のバージョンやそれらの組み合わせなどにより
　　　　　動作しないケースもありますので、予めご了承願います。


7. 実行エラー
	(1) 稼働時間外
		「
			error, e_api request error.
			p_errno:        [-62]
			p_err  :        [システム、情報提供時間外。]
		」

	(2) ログインエラー
		「
			error, e_api application error.
			sResultCode:    [10031]
			sResultText:    [ログイン前に行う電話番号認証が認証されない、または、ユーザID、暗証番号をお間違えです。]
		」

	(3) セッション無効化時
		「
			error, e_api request error.
			p_errno:        [2]
			p_err:		[セッションが切断しました。]
		」

8. その他
	本プログラムはご自身でｅ支店・ＡＰＩを利用したプログラムを開発するためのサンプルです。
	ｅ支店・ＡＰＩを利用したプログラム開発の際、参考資料の１つとしてご利用ください。
	※サンプルプログラムはお客様がｅ支店・ＡＰＩを利用したプログラム開発をする目的のみに利用を許諾致します。
	※サンプルプログラムは当社の都合により提供の停止を含む機能追加変更削除等を行いますので予めご了承ください。

	なお、
	サンプルプログラムはお客様がｅ支店・ＡＰＩを利用したプログラム開発をする目的で
	ｅ支店・ＡＰＩの使い方をその１例として参考提供することが目的ですので
	サンプルプログラム（として提供する全てのソースコード）の使用（含む一部機能（クラス、メソッド、
	サブルーチン等を含む）の利用や流用等）の
	実行により生じた全ての障害・損害・不具合等に関しては一切の責任を負いませんので、
	自己の判断、及び自己の責任においてご（参考）利用をお願いいたします。

以上。