# #76 marimo-embed spike package. Throwaway de-risk probes for the #64 construct
# ("marimo App.embed() as the strategy execution engine"). NOT runtime code — gated
# behind the `spike` dependency-group; never imported by engine/ or the kernel.
